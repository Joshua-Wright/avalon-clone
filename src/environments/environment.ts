// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBMW55NeHADgoLKusTsHPOJMCo40V6zhfI",
    authDomain: "hackavon.firebaseapp.com",
    databaseURL: "https://hackavon.firebaseio.com",
    projectId: "hackavon",
    storageBucket: "hackavon.appspot.com",
    messagingSenderId: "146580863075",
    appId: "1:146580863075:web:31047a1eab0dc1c68ea056",
    measurementId: "G-XFSF8RVPF2"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
